<UserControl x:Class="IconicLauncher.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock Text="WORKSHOP FOLDER" Style="{StaticResource SectionLabel}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding WorkshopFolder, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="1" Content="SCAN" Margin="10,0,0,0" MinWidth="90" Command="{Binding ScanCommand}"/>
            </Grid>
            <DataGrid Margin="0,14,0,0" Height="240" ItemsSource="{Binding ScanRows}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="FOLDER" Binding="{Binding Folder}" Width="220"/>
                    <DataGridTextColumn Header="NAME" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="PUBLISHED ID" Binding="{Binding PublishedId}" Width="130"/>
                    <DataGridCheckBoxColumn Header="DUPLICATE" Binding="{Binding Duplicate}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <ComboBox Width="300" ItemsSource="{Binding ServerTargets}" SelectedItem="{Binding SelectedServer}" DisplayMemberPath="Name"/>
                <Button Content="GENERATE JSON" Style="{StaticResource PrimaryButton}" Margin="12,0,0,0" Command="{Binding GenerateJsonCommand}"/>
                <Button Content="SAVE TO FILE" Margin="12,0,0,0" Click="OnSaveToFile"/>
            </StackPanel>
            <TextBox Margin="0,14,0,0" Height="200" Text="{Binding GeneratedJson, Mode=OneWay}" IsReadOnly="True" FontFamily="Consolas" FontSize="12"
                     TextWrapping="NoWrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
            <TextBlock Text="FTP PUBLISH" Style="{StaticResource SectionLabel}" Margin="0,20,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="HOST" Style="{StaticResource SectionLabel}"/>
                <TextBlock Grid.Column="1" Text="PORT" Style="{StaticResource SectionLabel}" Margin="10,0,0,6"/>
                <TextBlock Grid.Column="2" Text="USER" Style="{StaticResource SectionLabel}" Margin="10,0,0,6"/>
                <TextBlock Grid.Column="3" Text="PASSWORD" Style="{StaticResource SectionLabel}" Margin="10,0,0,6"/>
                <TextBlock Grid.Column="4" Text="REMOTE PATH" Style="{StaticResource SectionLabel}" Margin="10,0,0,6"/>
                <TextBox Grid.Row="1" Text="{Binding FtpHost, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="10,0,0,0" Text="{Binding FtpPort, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBox Grid.Row="1" Grid.Column="2" Margin="10,0,0,0" Text="{Binding FtpUser, UpdateSourceTrigger=PropertyChanged}"/>
                <PasswordBox Grid.Row="1" Grid.Column="3" Margin="10,0,0,0" x:Name="PwBox" PasswordChanged="OnPasswordChanged"/>
                <TextBox Grid.Row="1" Grid.Column="4" Margin="10,0,0,0" Text="{Binding RemotePath, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <Button Content="UPLOAD CONFIG" Style="{StaticResource PrimaryButton}" Command="{Binding UploadCommand}"/>
                <ProgressBar Width="200" Height="8" Margin="14,0,0,0" VerticalAlignment="Center" Maximum="100" Value="{Binding UploadProgress, Mode=OneWay}"
                             Visibility="{Binding IsUploading, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Text="{Binding AdminStatus}" VerticalAlignment="Center" Margin="14,0,0,0" Foreground="{StaticResource MutedBrush}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
